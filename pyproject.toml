[project]
name = "nhl-moment-to-market-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0,<3",
  "pandas>=2.2,<4",
  "statsmodels>=0.14,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
