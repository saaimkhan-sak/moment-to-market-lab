{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'Using committed validated static release'",
  "outputDirectory": "app",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/clubs/:slug", "destination": "/explore/" }
  ]
}
